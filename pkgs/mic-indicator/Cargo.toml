[package]
name = "mic-indicator"
version = "0.1.0"
edition = "2021"

[dependencies]
pipewire = "0.9"
tokio = { version = "1", features = ["rt", "macros", "sync"] }
zbus = { version = "5", default-features = false, features = ["tokio"] }
