[package]
name = "mic-indicator"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt", "macros", "time", "process"] }
zbus = { version = "5", default-features = false, features = ["tokio"] }
